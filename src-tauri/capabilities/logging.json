{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "logging",
  "description": "Log plugin permissions for macOS/Windows (plugin disabled on Linux CEF)",
  "platforms": ["macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "log:default"
  ]
}
