This will act as a changelog for the STL Slicer / AutoSupport project. Each entry will include a version number, a date, and a list of changes made during the current session. The version number will be based on the filename of the backup folder. These folders are incremental v1, v2, v3 etc. The updates should be newest to oldest versions.

## v106 - December 24, 2025
**Anatomy Preview: Cone Angle Tip Anchoring + Camera Presets + Tuner Workflow ✅**

### Trunk Preview: Cone Angle No Longer Shifts Trunk
- Preview-only adjustment so changing Adaptive/Locked cone angle keeps the trunk centered (socket stays centered) and moves the tip instead.

### Preview Camera: Presets as Single Source of Truth
- Removed duplicated raft home camera values in the canvas; initial framing and tuner reset now derive from the active kind’s home focus state.
- Updated/extended focus presets for Trunk + Raft under `Settings/AnatomyPreview/PreviewTypes/*/camera.ts`.

### Preview Tuner: Camera-Only + Auto Camera Toggle
- Removed support configuration controls from the tuner (camera/target only).
- Added an Auto Camera toggle so you can switch between “follow setting focus” and “1:1 locked camera” for copying values.

### Contact Diameter Focus: Keep Tip In View
- Contact Diameter focus now adjusts camera X framing so the tip stays in view even when cone angle shifts the tip left/right in the preview.

### Defaults
- Set default cone angle mode to Adaptive with a 60° offset for new/default support settings.

## v105 - December 23, 2025
**Contact Disk Dynamic Length: Joint Drag Stability + Export Fix ✅**

### Disk Length Override No Longer Camera/Drag Dependent
- Fixed an issue where contact disk length could snap to an extended value during/after joint gizmo drags and persist into STL export.
- Joint editing now avoids latching collision-based `diskLengthOverride` values so disk thickness is not influenced by unstable intermediate collision checks.

### Interaction + Collision Improvements
- OrbitControls are disabled while dragging a joint to prevent camera motion from affecting drag math.
- Collision raycasts were hardened against surface self-hits by adding a tiny ray-origin epsilon.

## v104 - December 22, 2025
**Line Raft: Clean Topology + Export Parity + Sidebar Save/Restore ✅**

### Line Raft Topology Cleanup (2D Union)
- Implemented 2D union + extrude for the Line Raft interior network to eliminate overlapping geometry.
- Fixed clipper-lib PolyTree traversal robustness and union fill rule behavior so overlaps merge instead of canceling.
- Final visual decision: keep **interior lines flat** and keep **chamfer only on the perimeter border ring** (avoids sloppy chamfer stitching artifacts).

### Export Parity (STL)
- Updated export pipeline to include Line Raft geometry:
  - Unioned interior line network
  - Chamfered perimeter border ring
  - Wall thickness correctly uses Line Height when Bottom Mode is Line
- Added export fallback: if union mesh is empty, export falls back to per-edge beam meshes so lines never disappear.

### Sidebar Save / Restore Defaults (LocalStorage)
- Added Save + Restore Defaults buttons to the bottom of the Support sidebar.
- Save persists support settings (existing store persistence) + raft settings to localStorage.
- Restore clears saved keys and resets both stores to built-in defaults.
- Added a short-lived Save confirmation message.
- Pinned the Save/Restore section as a true footer (content scrolls above it).

## v103 - December 22, 2025
**Modular Raft + Line Raft (Triangulated) ✅**

### Modular Raft Settings
- Refactored raft settings into a modular model:
  - Bottom mode: `off | solid | line`
  - Wall toggle (forced off when bottom is off)
- Updated UI to reflect new model and conditionally show relevant settings.

### Line Raft Geometry + Rendering
- Added a Line Raft renderer that generates:
  - Perimeter border ring
  - Interior line braces from Delaunay triangulation + pruning (no crossing)
- Added line raft parameters:
  - `Line W`, `Line H`, `Line Ch`
- Implemented line beam chamfer to match solid raft semantics (top wider, bottom inset).

### Perimeter Border + Wall Alignment
- Implemented chamfered perimeter border ring (manifold) and ensured wall sits on top of the border height.
- Expanded line-raft footprint to ensure the bottom of the chamfer still covers root disks (prevents disk protrusion).
- Prevented duplicate perimeter beams by excluding hull edges when the border ring is present.

### Files Modified / Added
- `src/supports/Rafts/Crenelated/RaftTypes.ts`
- `src/supports/Rafts/Crenelated/RaftDefaults.ts`
- `src/supports/Rafts/Crenelated/RaftState.ts`
- `src/supports/Rafts/Crenelated/rendering/LineRaftRenderer.tsx`
- `src/supports/Rafts/Crenelated/geometry/generateChamferedBeam.ts`
- `src/supports/Rafts/Crenelated/geometry/generatePerimeterBorderBeam.ts`
- `src/supports/Rafts/Crenelated/geometry/delaunayTriangulate2d.ts`
- `src/supports/Rafts/Crenelated/geometry/convexHull2d.ts`
- `src/components/scene/SceneCanvas.tsx`
- `src/supports/Settings/components/RaftSettingsCard.tsx`

## v102 - December 22, 2025
**Raft Preview: Focus Cameras + Palette Highlighting + Tuner Unlock ✅**

### Raft Setting Focus → Camera Views
- Raft settings inputs now drive the anatomy preview focus system via setting keys:
  - `raft.thickness`
  - `raft.chamferAngle`
  - `raft.wallHeight`
  - `raft.wallThickness`
  - `raft.crenulationGapWidth`
- Added camera poses for each focus key (position/target/zoom) using the Preview Tuner.
- Raft tab home camera now stays consistent by using a raft-specific home focus state when nothing is focused.

### Raft Preview Highlighting (Palette)
- Raft preview recolors now use `ANATOMY_CONFIG.colors` (normal/dim/highlight).
- Focus highlighting rules:
  - Thickness + Chamfer: base highlighted, wall dimmed.
  - Wall Height + Wall Thickness + Gap Width: wall highlighted, base dimmed.

### Preview Tuner / Camera Interaction Fixes
- Camera auto-animator no longer “locks” the tuner while orbiting: OrbitControls interaction now stops the animator.
- Preview Tuner camera **Pos Z** is no longer forced to 10:
  - Pos Z slider unlocked.
  - Manual camera updates apply full XYZ.
  - Copy-to-clipboard includes current Z.

### Files Modified
- `src/supports/Settings/SupportSidebar.tsx`
- `src/supports/Settings/components/RaftSettingsCard.tsx`
- `src/supports/Settings/AnatomyPreview/SupportAnatomyPreviewCanvas.tsx`
- `src/supports/Settings/AnatomyPreview/AnatomyPreviewCameraLogic.ts`

## v101 - December 21, 2025
**Anatomy Preview: Granular Highlights + Tuner + Config Unification ✅**

### Granular Anatomy Highlighting
- **Component-Level Visuals:** Implemented granular highlights for discrete parts:
  - **Tip Contact:** Pink when editing `Tip Contact` settings.
  - **Cone Body:** Pink when editing `Cone Length`/`Angle`.
  - **Roots Disk:** Pink when editing `Disk Height` / `Diameter`.
  - **Roots Cone:** Pink when editing `Cone Height`.
  - **Shaft/Joints:** Pink when editing `Shaft` or `Joints`.
- **Context Dimming:** Non-active parts fade to Light Grey to emphasize the focused component.

### Configuration & Tuner
- **Unified Configuration:** Centralized all preview constants (colors, camera defaults, lighting) into `src/supports/Settings/AnatomyPreview/AnatomyPreviewConfig.ts` for easy tweaking.
- **Preview Tuner:** Renamed "Debug UI" to "Preview Tuner" and exposed an explicit toggle in the config.
- **Reactivity Fix:** Fixed Contact Cone Body size not updating when changing Shaft Diameter (it now correctly links to shaft diameter).

### Files Modified
- `src/supports/Settings/AnatomyPreview/AnatomyPreviewConfig.ts`
- `src/supports/Settings/AnatomyPreview/SupportAnatomyPreviewCanvas.tsx`
- `src/supports/SupportPrimitives/Roots/RootsRenderer.tsx`
- `src/supports/SupportTypes/Trunk/trunkBuilder.ts`
- `src/supports/rendering/SupportBuilder.tsx`
## v101 - December 21, 2025
**Anatomy Preview: Granular Highlights + Tuner + Config Unification ✅**

### Support Previews No Longer Break Mouse Hover
- Fixed a placement flicker where the support preview could cross the cursor ray and steal the hover hit, causing the model hit to drop out and the preview to disappear until the mouse moved again.
- Preview geometry is now treated as non-interactive for hover targeting:
  - Preview contact disk tip sphere now respects the preview raycast override.
  - Preview shafts opt out of GPU picking registration so they cannot be picked instead of the model.
  - Temporary hover/tip marker spheres are non-raycastable.

### Files Modified
- `src/supports/SupportPrimitives/ContactDisk/ContactDiskRenderer.tsx`
- `src/supports/SupportPrimitives/Shaft/ShaftRenderer.tsx`
- `src/supports/rendering/SupportBuilder.tsx`
- `src/components/scene/SceneCanvas.tsx`

## v98 - December 17, 2025
**Global Grid Toggle + Stable Trunk Baseline Diameter ✅**

### Grid Toggle Is Global (Not Per Preset)
- Switching presets no longer overrides `grid.enabled`.
- Once Grid is toggled ON, it stays ON across preset changes until explicitly turned OFF (and vice versa).

### Adaptive Trunk: Stable Baseline Diameter
- Trunks now store a baseline shaft diameter at creation (`baseDiameterMm`).
- Adaptive stepwise trunk profile uses the trunk’s own baseline instead of the currently active preset.
- Fixes cases where placing a thin trunk first, then switching to a thicker preset to place a branch would incorrectly make the trunk thick all the way to the tip.

### Files Modified
- `src/supports/Settings/presets.ts`
- `src/supports/types.ts`
- `src/supports/SupportTypes/Trunk/trunkBuilder.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/maxConnectedDiameter.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/applyTrunkReplacement.ts`

## v97 - December 17, 2025
**Adaptive / Stepwise Trunk Diameter + Trunk Delete/Promote Fixes ✅**

### Stepwise Trunk Diameter (Branch-Driven Demand)
- Trunk diameter is now computed as a **stepwise profile** (thin above, thicker below), based on **trunk-hosted branch knots**.
- Step boundaries are defined by the **trunk-hosted knot height** (knot position), not the branch tip contact.
- If demand sources are removed, the trunk profile **recomputes immediately** (it can shrink back down).

### Transition Joint Sizing Fix
- Transition joints at diameter steps are now sized based on the **larger adjacent shaft** (joint = larger + 0.1mm), not the smaller.

### Branch Deletion Updates Trunk Profile (Undo-safe)
- Deleting a branch now recomputes the affected trunk’s diameter profile so the trunk shrinks to the next-largest remaining demand.
- Undo/Redo includes any trunk/knot updates required for split/rehost operations.

### Trunk Deletion: Promote Branch Without Diameter/Tip Corruption
- When deleting a trunk and promoting a direct child branch:
  - The replacement trunk baseline diameter now inherits the **promoted branch diameter** (avoids “small branch becomes large” issues).
  - The replacement trunk preserves the promoted branch’s **contact cone profile**.
  - The replacement trunk’s **socket joint** is re-positioned after cone profile swap so the trunk remains correctly linked to the cone.

### Files Modified
- `src/features/supports/useSupportInteractionManager.ts`
- `src/supports/history/actionTypes.ts`
- `src/supports/history/useSupportHistoryHandlers.ts`
- `src/supports/SupportTypes/Trunk/useTrunkPlacement.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/maxConnectedDiameter.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/applyTrunkReplacement.ts`

## v96 - December 17, 2025
**Trunk Deletion: Promote Next-Highest Branch Into New Trunk (Undo-safe) ✅**

### Delete Trunk => Automatic Promotion
- Deleting a trunk now attempts to promote the **highest-contact direct child branch** into a new trunk.
- Uses the existing TrunkReplacement planner+apply pipeline so the operation is atomic and avoids broken references.
- If no eligible direct child branch exists, deletion falls back to the existing safe cascade trunk removal.

### Behavior Details
- Promotion candidate selection is constrained to **direct child branches** (branches attached to knots hosted on the trunk).
- When deleting a trunk, we do **not** preserve the old trunk contact as a branch (since the trunk was explicitly deleted).
- Undo/Redo is a single step via snapshot-based `SUPPORT_REPLACE_TRUNK` history.

### Files Modified
- `src/features/supports/useSupportInteractionManager.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/planTrunkReplacement.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/applyTrunkReplacement.ts`

## v95 - December 17, 2025
**Grid Trunk Promotion + Trunk Replacement (Undo-safe) + Diameter Propagation + Settings Crash Fix ✅**

### Grid: Tallest Contact Wins Trunk
- Grid placement now detects when a new contact at a node is higher than the existing trunk contact.
- In that case, placement performs a trunk replacement instead of adding a branch.
- The old trunk contact is preserved by converting it into a branch attached to the new trunk.

### Trunk Replacement Module (Planner + Apply)
- Added reusable trunk-domain module under `src/supports/SupportTypes/Trunk/TrunkReplacement/`.
- Replacement execution rehosts dependents onto the new trunk before removing the old trunk to avoid cascade-delete side effects.
- Undo/Redo is single-step via full before/after snapshot history.

### Safety Constraints (No "Knot Above Tip" Islands)
- Rehosted attachment selection enforces below-tip and minimum steepness constraints.

### Thickest Member Wins (Diameter Propagation)
- Added connected-graph diameter scan and trunk diameter application helpers.
- Trunk diameter is set from the max member diameter across the connected tree.
- Branch placement can thicken the host trunk and records the trunk update in the same history step.

### Settings UI Crash Fix (NumberInput)
- Fixed `NumberInput` crash when receiving undefined numeric values.
- Normalized settings merge so Grid settings always have complete numeric defaults (including presets + localStorage).

### Files Modified
- `src/supports/PlacementLogic/Grid/types.ts`
- `src/supports/PlacementLogic/Grid/gridPlacement.ts`
- `src/supports/SupportTypes/Trunk/useTrunkPlacement.ts`
- `src/supports/SupportTypes/Trunk/TrunkReplacement/*`
- `src/supports/state.ts`
- `src/supports/history/actionTypes.ts`
- `src/supports/history/useSupportHistoryHandlers.ts`
- `src/supports/Settings/state.ts`
- `src/supports/Settings/presets.ts`
- `src/components/ui/NumberInput.tsx`

## v94 - December 16, 2025
**Camera Focus Hotkey Refactor (F Refocus) ✅**

### Camera Focus Ownership
- Moved camera-focus behavior out of `SceneCanvas.tsx` and into the scene camera module.
- Kept hotkey wiring in `src/hotkeys/` while moving camera control logic to `src/components/scene/camera/`.

### Files Modified
- `src/components/scene/SceneCanvas.tsx`
- `src/components/scene/camera/CameraFocusHotkeyController.tsx`
- `src/components/scene/camera/index.ts`
- `src/hotkeys/hotkeyConfig.ts`
- `src/hotkeys/useCameraFocusHotkey.ts`

## v93 - December 16, 2025
**STL Load Camera: Correct Orientation + Smooth Fit-to-View Intro ✅**

### Camera Default Orientation
- Updated the default scene camera view so newly loaded STLs start from the expected “front” direction.

### Camera Intro Animation (Load / First Model)
- Added a smooth (~1s) camera intro that frames the loaded model so it fits fully in view.
- Uses bounds-based distance so it works consistently for any model size.
- Refined the trigger timing to remove first-frame stutter and eliminated target snaps to world origin.

### Files Modified
- `src/components/scene/SceneCanvas.tsx`

## v92 - December 16, 2025
**Grid Snap Refinements + Branch Edit Parity ✅**

### Grid Placement (Better Node Choice)
- Grid snapping now targets the grid node closest to the **contact socket (shaft-to-tip joint)** XY, instead of the trunk root/base XY.
  - This helps avoid cases where supports appear to “lean backward” due to snapping to a cell that is not directly under the contact joint.
- Grid index rounding is now symmetric for negative coordinates to avoid subtle half-step bias around the origin.

### Branch Tip Orientation (Cone Angle Mode)
- Branch building now applies **Cone Angle Mode** (Normal/Locked/Adaptive) by resolving the cone axis via `ConeAxisPolicy`.
  - This applies to all branches, including Grid-created branches.

### Branch Joint Deletion (Delete Key)
- Joint deletion now supports **branches** (previously trunk-only): deleting a selected branch joint merges adjacent segments and rebinds hosted knots.
- Added branch update history support so Undo/Redo works for branch joint deletion.

### Files Modified
- `src/supports/PlacementLogic/Grid/gridPlacement.ts`
- `src/supports/PlacementLogic/Grid/gridMath.ts`
- `src/supports/SupportTypes/Branch/branchBuilder.ts`
- `src/supports/state.ts`
- `src/features/supports/useSupportInteractionManager.ts`
- `src/supports/history/actionTypes.ts`
- `src/supports/history/useSupportHistoryHandlers.ts`
- `src/supports/Grid/gridMath.ts`
- `src/supports/Grid/gridPlacement.ts`
- `src/supports/Grid/types.ts`

## v91 - December 16, 2025
**Trunk Deletion: Safe Cascade Delete + Undo/Redo ✅**

### Trunk Deletion Behavior
- Deleting a trunk now performs cascade deletion so no broken references remain.
- Removed dependents include:
  - Knots hosted on trunk segments
  - Branches attached to those knots (including downstream cascade)
  - Leaves attached to those knots
  - Braces using those knots

### History / Undo-Redo
- Trunk deletion history now stores all removed dependent entities so undo restores the full supports graph safely.

### Files Modified
- `src/supports/state.ts`
- `src/features/supports/useSupportInteractionManager.ts`
- `src/supports/history/actionTypes.ts`
- `src/supports/history/useSupportHistoryHandlers.ts`
- `1. Documentation/FeatureList.md`

## v90 - December 15, 2025
**Conditional Knot Visibility (Selected Support) ✅**

### Knot Rendering
- When nothing is selected, all knots are visible.
- When any support element is selected, only knots belonging to the effectively-selected support remain visible.

### Files Modified
- `src/supports/SupportRenderer.tsx`
- `src/supports/SupportTypes/Branch/BranchRenderer.tsx`
- `src/supports/SupportTypes/Leaf/LeafRenderer.tsx`
- `src/supports/SupportTypes/Brace/BraceRenderer.tsx`

## v89 - December 15, 2025
**Support Selection Dimming (Viewport) ✅**

### Support Selection Visuals
- When any support element is selected, all other supports render in a dim grey while the selected support remains cyan.
- Implemented by threading a `dimNonSelected` flag from `SupportRenderer` into each support type renderer and overriding the `useHighlight` base color for non-selected supports.

### Files Modified
- `src/supports/SupportRenderer.tsx`
- `src/supports/SupportTypes/Trunk/TrunkRenderer.tsx`
- `src/supports/SupportTypes/Branch/BranchRenderer.tsx`
- `src/supports/SupportTypes/Leaf/LeafRenderer.tsx`
- `src/supports/SupportTypes/Brace/BraceRenderer.tsx`
- `src/supports/SupportTypes/Twig/TwigRenderer.tsx`
- `src/supports/SupportTypes/Stick/StickRenderer.tsx`

## v88 - December 15, 2025
**Alt Preview Responsiveness + Brace Click Feedback ✅**

### Preview Performance (Reduced Frame Drops)
- Improved preview smoothness by avoiding per-update cylinder geometry rebuilds.
- Shafts now render a unit-height cylinder and scale it to the desired length.

### Brace Placement (Immediate Click Response)
- Removed the perceived delay after Alt+click when starting brace placement by setting a zero-length preview immediately on click (rather than waiting for the next `useFrame` tick).

### Files Modified
- `src/supports/SupportPrimitives/Shaft/ShaftRenderer.tsx`
- `src/supports/SupportTypes/Brace/BracePreviewRenderer.tsx`
- `src/supports/SupportTypes/Brace/BracePlacementController.tsx`
- `1. Documentation/ARCHITECTURE_AND_HANDOFF.md`

## v87 - December 15, 2025
**Twig/Stick Interaction: Joint Rendering + Attachment Robustness ✅**

### Twig Joint Display
- Fixed Twig joints not displaying by rendering Twig segment joints via the shared `JointRenderer` (matching Stick/Trunk/Branch behavior).

### Twig/Stick Editing & Attachment (In Progress)
- Continued extending the editing pipeline to support Twig/Stick:
  - shaft snapping targets for joint/brace interactions
  - knot attachment + sliding on Twig/Stick shafts
  - recompute dependent geometry (knots/leaves/braces) when Twig/Stick shafts are edited

### Files Modified
- `src/supports/SupportTypes/Twig/TwigRenderer.tsx`
- `src/supports/SupportPrimitives/Knot/useKnotInteraction.ts`
- `src/supports/SupportPrimitives/Knot/KnotGizmo.tsx`
- `src/supports/SupportTypes/Brace/BracePlacementController.tsx`
- `src/supports/SupportPrimitives/Joint/useJointCreation.ts`
- `src/supports/SupportPrimitives/Joint/jointUtils.ts`
- `src/supports/SupportPrimitives/Joint/JointGizmo.tsx`
- `src/supports/Curves/BezierGizmo/BezierGizmoManager.tsx`
- `src/supports/interaction/SnappingManager.ts`
- `src/supports/state.ts`

## v86 - December 14, 2025
**Brace: True Shaft Primitives + Curvature + Manifold Taper + Knot Drag Fixes ✅**

### Brace Shaft Rendering (No bespoke mesh)
- Refactored `BraceRenderer` to render the brace shaft using the shared shaft primitives:
  - Straight: `SupportPrimitives/Shaft/ShaftRenderer.tsx`
  - Curved: `Renderers/BezierRenderer.tsx`
- Removed the stitched-cylinder curved brace implementation that caused visible seams.

### Curved Brace = Single Tapered Tube (Manifold)
- Upgraded `BezierRenderer` to support `diameterStart` / `diameterEnd`.
  - Implementation uses one `TubeGeometry` and scales each ring’s vertices along the curve to apply taper.
- Upgraded `ShaftRenderer` to support optional `diameterStart` / `diameterEnd` for straight taper.

### Curve UX & Selection
- Restored Bezier handle visibility: handles show for selected curved elements (no need to hold `C`).
- Enabled `C` toggling for braces when brace is selected (no secondary segment selection required).
- Updated selection logic so `braceSegment:<braceId>` counts as selecting the brace for highlight/editability.

### Universal Knot Attachment: Brace + Curved Host Dragging
- Fixed cases where knots attached to brace shafts were missing `t`, preventing recompute-on-edit.
  - Enforced `t` presence for shaft snaps in leaf placement (brace shaft snaps).
- Fixed knot dragging so knots hosted on Bezier trunk/branch segments project onto the curve during drag (not the straight chord).
  - Prevents “break free while dragging, then snap back after next curve edit”.

### Files Modified
- `src/supports/Curves/BezierGizmo/BezierGizmoManager.tsx`
- `src/supports/SupportTypes/Brace/BraceRenderer.tsx`
- `src/supports/Renderers/BezierRenderer.tsx`
- `src/supports/SupportPrimitives/Shaft/ShaftRenderer.tsx`
- `src/supports/SupportRenderer.tsx`
- `src/supports/SupportTypes/Leaf/LeafPlacementController.tsx`
- `src/supports/SupportPrimitives/Knot/useKnotInteraction.ts`

## v85 - December 13, 2025
**Universal Snapping + Global Knot Attachment (Branch/Leaf-ready, Brace-ready) ✅**

### Universal Snapping (Placement)
- Updated **Branch placement** to follow the same **universal snapping state machine** used by Leaf.
- Branch placement now requires a **locked snap target** (no more free-floating base), so every placed Branch is truly attached to an existing shaft.
- Branch snap targets are now **Bezier-aware** (snap geometry includes control points when a segment is Bezier).

### Global Knot Attachment (Reshape-safe)
- Knots now stay attached through shaft edits by relying on the universal contract:
  - `parentShaftId` + `t` (0..1 along segment) is the authoritative attachment.
- Branch-created knots now store `t` and host diameter data so they remain stable during:
  - joint deletion (segment merge)
  - bezier <-> straight toggles
  - shaft reshaping
- Implemented knot rebind on **joint deletion** so knots on the removed segment are projected onto the merged segment instead of drifting.
- Extended state mutation routes so **branch edits** also recompute knot positions for knots attached to branch segments.

## v84 - December 13, 2025
**Cone Angle Modes (Normal/Locked/Adaptive) & Adaptive Offset ✅**

### Support Placement (Trunk Tip)
- Added **Cone Angle Mode** setting for the contact tip:
  - **Normal**: preserves current behavior (including the near-wall 90–95° anti-horizontal tilt).
  - **Locked**: snaps cone axis to stability target angles in defined surface-angle bands.
  - **Adaptive**: smoothly biases the cone axis toward more vertical; magnitude is user-controlled.
- Added **Adaptive Offset (deg)** setting (default 30) to control the max bias amount for Adaptive mode.
- Disk/contact point remains at the clicked position (surface normal alignment), while the **cone axis** may be modified for stability.

### Architecture
- Encapsulated cone-axis behavior in a dedicated policy module:
  - `src/supports/PlacementLogic/ConeAxisPolicy.ts`
- Wired policy into:
  - `src/supports/PlacementLogic/StandardPlacement.ts` (authoritative axis + min angle rules)
  - `src/supports/PlacementLogic/SmartPlacement.ts` (preserves cone axis through collision-solving)
  - `src/supports/SupportTypes/Trunk/trunkBuilder.ts` (uses policy axis/socket for final geometry)

### Settings UI
- Updated Tip settings UI to use a dropdown for Cone Angle Mode and to show Adaptive Offset only when Adaptive is selected.

## v83 - December 10, 2025
**Seed Voxel Visualization & Step 3/4 Merge ✅**

### Core Logic
- **Merged Steps**: Merged "Step 4: Seed Voxel Assignment" into "Step 3".
  - **Seed Output**: Step 3 (Center Finder) now outputs both the floating-point `internalCenter` (for visuals) AND the integer `seedVoxel` (grid coordinates for expansion).
- **Data Structure**: Updated `Island` interface to include `seedVoxel: {x,y,z}`.

### Visualization
- **Seed Visualization**: Implemented "Green Box" visualization for the exact seed voxel.
  - **Always On Top**: Renders with `depthTest: false` so the seed is visible deep inside the mesh.
  - **Independent Toggles**: Split the UI into two separate buttons:
    - **Centers**: Toggles Yellow Spheres (Geometric Center).
    - **Seeds**: Toggles Green Voxels (Expansion Seeds).
  - **Logic**: Added `computeSeedMarkers` to generate correct world-space geometry for the seeds using the scan grid parameters.

### UI Polish
- **Analysis Card**: Added the "Seeds" toggle button to the Step 3 section.
- **Hook State**: Updated `useIslandVolumeAnalysis` to manage `showSeeds` independent of `showCenters`.

### Files Modified
- `src/volumeAnalysis/IslandScan/islandOverlayLogic.ts` (New marker logic)
- `src/volumeAnalysis/islandVolume/steps/Step3_InternalCenter.ts` (Populate seed)
- `src/volumeAnalysis/islandVolume/useIslandVolumeAnalysis.ts` (State & Toggles)
- `src/volumeAnalysis/islandVolume/components/IslandVolumeAnalysisCard.tsx` (UI)
- `src/app/page.tsx` (Rendering)
- `src/components/scene/IslandOverlay.tsx` (Green material)
- `Scratch/Island_Volume_Analysis_Dev_Plan.md` (Step merge)

## v82 - December 10, 2025
**Massive Performance Optimization (2s Render) & Center Point Fix ✅**

### Performance "Step Change"
- **RLE Greedy Meshing**: Replaced the O(Voxel) mesh generator with an O(Segment) implementation directly on RLE data.
  - **Speedup**: Mesh generation dropped from ~30s to <2s for complex models.
  - **Memory**: Utilized Pre-allocated TypedArrays and Zero-GC masking to eliminate garbage collection pauses.
  - **Optimization**: Hoisted neighbor lookups to O(1) inside the loop.
- **Linear Island Analysis**:
  - **Scan Fix**: Replaced the O(N^2) connectivity check in `rle.ts` with an O(N) sliding window scan.
  - **Overlap Fix**: Replaced the quadratic overlap detection in `IslandTracker` (Scanning K components * H rows) with a linear Single-Pass scan (O(Total Runs)).
  - **Result**: Island analysis is now scale-invariant and instantaneous.

### Bug Fixes
- **Center Point "Top Bias"**: Fixed a bug where the Internal Center calculation (Step 3) was biased towards the very top of the island.
  - **Cause**: The Distance Transform treated the top boundary of the grid as a "Wall" (Neumann) instead of "Empty Space" (Dirichlet), causing the top layer to think it was "deep inside" the volume.
  - **Solution**: Added explicit boundary clamping in the Chamfer Distance Transform to force distance to go to 0 at the grid edges.
- **Backface Culling**: Fixed winding order in `RleMeshing.ts` to allow standard Backface Culling, improving render performance.

### Logic
- **Dev Plan Update**: Updated `Island_Volume_Analysis_Dev_Plan.md` to reflect "Optimization Checkpoint 2".

### Files Modified
- `src/volumeAnalysis/VoxelSystem/RleMeshing.ts` (Rewritten)
- `src/volumeAnalysis/islandVolume/steps/voxelization/islandTracker.ts` (Optimized)
- `src/volumeAnalysis/islandVolume/steps/voxelization/rle.ts` (Optimized)
- `src/volumeAnalysis/islandVolume/steps/Step3_InternalCenter.ts` (Fixed)
- `Scratch/Island_Volume_Analysis_Dev_Plan.md` (Updated)

## v81 - December 10, 2025
**Island Volume Analysis Step 3 Implementation & Fixes ✅**


### Core Logic
- **Step 3 Implementation**: Implemented "Internal Center" (Pole of Inaccessibility) calculation for island volumes.
  - **Algorithm**: Used 3D Chamfer Distance Transform (3-4-5 weights) to find the point deepest inside the island.
  - **Coordinate Correction**: Robustly fixed coordinate mapping to ensure center points align correctly with the Z-up world space (fixing a Y/Z axis swap issue).

### UI & Visualization
- **Step 3 Controls**: Added "Identify Centers" button and visibility toggle to the `IslandVolumeAnalysisCard`.
- **Visualization**:
  - **Center Dots**: Visualized internal centers as yellow spheres.
  - **Visibility**: Increased dot size to 3x voxel diameter for better visibility in the scene.

### Files Modified
- `src/volumeAnalysis/islandVolume/steps/Step3_InternalCenter.ts` (New logic)
- `src/volumeAnalysis/islandVolume/useIslandVolumeAnalysis.ts` (Integration)
- `src/volumeAnalysis/islandVolume/components/IslandVolumeAnalysisCard.tsx` (UI)
- `src/app/page.tsx` (Visualization)
- `src/volumeAnalysis/islandVolume/voxelization/types.ts` (Type definitions)

## v80 - December 10, 2025
**Documentation Sync & UI Defaults ✅**

### UI & UX
- **Red Overlays Default**: Changed "Red Overlays" (Start Points) to be **OFF** by default to reduce visual clutter.
- **Button Label**: Renamed "Starts" button to "Red Overlays" for clarity.

### Documentation
- **Plan Alignment**: Updated `Island_Volume_Analysis_Dev_Plan.md` to match the App's workflow numbering.
  - Merged Dev Steps 2-4 into **App Step 2** (Island Analysis).
  - Promoted "Internal Center Calculation" to **App Step 3**.

### Files Modified
- `src/volumeAnalysis/islandVolume/useIslandVolumeAnalysis.ts` (Defaults)
- `src/volumeAnalysis/islandVolume/components/IslandVolumeAnalysisCard.tsx` (UI labels)
- `Scratch/Island_Volume_Analysis_Dev_Plan.md` (Documentation)

## v79 - December 10, 2025
**Voxel-First Refactor & Stool Leg Logic ✅**

### Core Logic Refactor
- **Voxel-First Workflow**: Swapped the analysis order. Step 1 is now pure Voxelization (Solid Scan), and Step 2 analyzes that voxel data.
- **"Stool Leg" Analysis Logic**: Implemented "Body Dominant" island detection in `IslandTracker`.
  - **Rule**: Distinct overhangs (Legs) get unique IDs.
  - **Merge = Stop**: When an island merges with the body or another island, it stops propagating its unique ID and becomes part of the "Body" (ID 1).
  - **Result**: Visualizes delicate overhangs as distinct volumes while leaving the robust main body neutral.

### UI Improvements
- **Red Overlay Toggle**: Added a dedicated visibility toggle for "Red Overlays" (Start Markers) in the Analysis Card.
  - **Default**: OFF by default to keep the view clean.
  - **Label**: Renamed from "Starts" to "Red Overlays" for clarity.

### Code Cleanup
- **Duplicate Removal**: Removed redundant `Step1_BasicScan.ts`. Reused `ScanOrchestrator` and `IslandTracker` logic for the analysis step.
- **IslandTracker**: Completely rewrote `IslandTracker.ts` to implement the simplified "Body Dominant" merge logic, removing complex parent tracking.

### Files Modified
- `src/volumeAnalysis/islandVolume/steps/voxelization/islandTracker.ts` (Logic rewrite)
- `src/volumeAnalysis/islandVolume/useIslandVolumeAnalysis.ts` (Workflow swap)
- `src/volumeAnalysis/islandVolume/steps/Step2_Analysis.ts` (New analysis step)
- `src/volumeAnalysis/islandVolume/components/IslandVolumeAnalysisCard.tsx` (UI updates)
- `src/volumeAnalysis/islandVolume/steps/voxelization/ScanOrchestrator.ts` (Orchestration updates)

## v78 - December 10, 2025
**Island Volume Analysis Step 1 Fixes & Progress UI ✅**

### Bug Fixes
- **Visualization Alignment**: Fixed critical issue where "lowest point" markers were misaligned (wrong orientation/position) relative to the model.
  - **Coordinate Correction**: Updated `Step1_BasicScan` to return `IslandMarker[]` with full `Geometry`, using the exact same coordinate math as `IslandOverlay`.
  - **Transform Logic**: Updated `useIslandVolumeAnalysis` to scan the *transformed* world geometry (Rotation/Scale applied) instead of raw mesh.
  - **Double-Transform Prevention**: Adjusted marker positions in the hook to compensate for `IslandOverlay`'s dynamic model-position offset, ensuring markers appear exactly at the island tips.

### Features
- **Progress Tracking**: Added a visual progress bar to the `IslandVolumeAnalysisCard` so users can see scan status (0-100%).
- **Hook Code cleanup**: Removed lingering Markdown artifacts and cleaned up syntax in `useIslandVolumeAnalysis`.

### Files Modified
- `src/volumeAnalysis/islandVolume/steps/Step1_BasicScan.ts` (Logic update)
- `src/volumeAnalysis/islandVolume/useIslandVolumeAnalysis.ts` (Hook update)
- `src/volumeAnalysis/islandVolume/components/IslandVolumeAnalysisCard.tsx` (UI update)
- `src/app/page.tsx` (Integration update)

## v77 - December 10, 2025
**Volume Analysis Refactor & UI Polish ✅**

### Refactoring
- **Volume Analysis Structure**: Consolidated `IslandScan`, `VoxelSystem`, and `TerritorySystem` into a new `src/VolumeAnalysis/` directory.
- **Clean Architecture**: Moved related logic from `modules/island`, `features/islands`, and `workers/` into the new domain-driven structure.
- **Worker Updates**: Updated `islandScan.worker.ts` and `scanlineScan.worker.ts` imports to align with the new structure.

### UI Improvements
- **Analysis Tab**: Added a dedicated "Analysis" tab to the application interface.
- **Component Relocation**: Moved Island/Voxel control cards (`IslandScanCard`, `IslandOverlayControls`, `IslandListCard`) to the Analysis tab.
- **Voxel Controls Split**: Split `IslandVoxelControls` into two distinct cards:
  - **Island Voxels**: For basic island visualization settings.
  - **Territory Voxels**: For territory-specific settings and surface priority mode.
- **Icon Polish**: Replaced generic "eyeball" icons with intuitive Chevron icons (Right=Collapsed, Blue Down=Expanded).

### Files Modified
- Moved many files into `src/VolumeAnalysis/`
- `src/components/controls/TerritoryVoxelControls.tsx` (Created)
- `src/components/controls/IslandVoxelControls.tsx` (Refactored)
- `src/components/controls/IslandScanCard.tsx` (Icon update)
- `src/components/controls/IslandOverlayControls.tsx` (Icon update)
- `src/components/controls/IslandListCard.tsx` (Icon update)
- `src/app/page.tsx` (Tab logic, Voxel control logic)
- `src/components/layout/TopBar.tsx` (Added Analysis button)

## v76 - December 7, 2025
**Branch Placement UX Improvements ✅**

### New Features
- **Alt-Hold Preview Dot**: When holding Alt key, a green preview dot now appears following the mouse on the model surface, showing where the branch tip will be placed
- **Preview dot size**: Uses the tip contact diameter from settings (0.3mm by default)

### Bug Fixes
- **Branch Preview Ghost**: Fixed persistent preview after branch placement using `justFinalized` flag to block useFrame from re-setting preview data
- **Alt Release Cancels Branch Mode**: Releasing Alt key now properly cancels branch placement and returns to trunk mode

### Improvements
- **Hotkey Configuration**: Branch placement hotkey (Alt) is now defined in `hotkeyConfig.ts` for future customization

### Files Modified
- `src/supports/SupportTypes/Branch/branchPlacementState.ts` - Added hoverPosition, justFinalized flag, finalize() method
- `src/supports/SupportTypes/Branch/useBranchPlacement.ts` - Added hover tracking, Alt release reset, hotkey config usage
- `src/components/scene/SceneCanvas.tsx` - Added branchHoverPosition prop and preview dot rendering
- `src/app/page.tsx` - Pass hoverPosition to SceneCanvas
- `src/hotkeys/hotkeyConfig.ts` - Added BRANCH_PLACEMENT hotkey entry

## v75 - December 5, 2025
**Knot Movement Constraint Fix ✅**

### Bug Fix
- **Geometry Inversion Prevention**: Fixed a critical bug where dragging a knot too high would cause the branch to disconnect from the contact cone. The joints were being pushed above the socket, inverting the branch geometry.

### Root Cause
- The elastic chain solver was only checking angle constraints, not topology constraints
- When the last joint was pushed ABOVE the socket Z position, the segment direction reversed (what was an upward segment became downward)
- The angle check passed because vertical distance was still adequate, but the geometry was inverted

### Solution
- Added **topology constraint check**: If the socket was originally above the last joint, the last joint can never be pushed above the socket
- Back-propagation now clamps joints and the knot to prevent crossing the socket boundary
- Fixed horizontal distance calculation to use INITIAL positions (not current moving positions)
- Added socket position import using `getSocketPosition()` instead of raw tip position

### Files Modified
- `src/supports/PlacementLogic/ElasticChainSolver.ts` - Added topology violation check and back-propagation
- `src/supports/SupportPrimitives/Knot/KnotGizmo.tsx` - Use socket position for constraint
- `src/supports/SupportPrimitives/Knot/useKnotInteraction.ts` - Use socket position for constraint

## v74 - December 5, 2025
**Branch Preview Ghost Fix ✅**

### Bug Fix
- **Lingering Preview**: Fixed a bug where the branch placement preview (ghost) would remain visible after placing a branch until the user moved their mouse back over the branch.
- **Root Cause**: The preview data wasn't being cleared immediately when the branch was finalized, causing a 1-frame delay where the old preview was still rendered.
- **Solution**: Added explicit `setPreviewData(null)` call in `BranchPlacementController` before calling `reset()` to ensure the preview is cleared synchronously.

### Files Modified
- `src/supports/SupportTypes/Branch/BranchPlacementController.tsx` - Added explicit preview clear on branch creation

## v73 - December 5, 2025
**Elastic Chain Drag Implementation ✅**

### Core Feature
- **Elastic Chain Behavior**: When dragging a knot upwards on a trunk, attached branch joints automatically push upwards to maintain the maximum shaft angle constraint.
- **Angle-Based Constraints**: Joints are pushed only when needed to maintain the user's configured `maxAngleDeg` setting (default 80°).
- **Reversible Movement**: Joints return to their original positions when the knot is dragged back down (elastic behavior).
- **Fixed Tip Constraint**: If a branch has a ContactCone (fixed tip touching the model), the system back-propagates constraints to prevent over-extension.

### Technical Implementation
- **New Solver**: Created `ElasticChainSolver.ts` in `PlacementLogic/` with:
  - `ElasticChainInitialState` interface for capturing branch state at drag start
  - `solveElasticChain()` function that calculates new joint positions based on angle constraints
  - Forward pass (push joints up) and back-propagation (tip constraint clamping)
- **KnotGizmo Integration**: Integrated elastic chain logic directly into `KnotGizmo.tsx`:
  - State captured on pointer down (knot position, joint positions, tip position)
  - Solver runs every frame during drag
  - Branch joints updated in state after each solver pass
- **Direction-Aware Logic**: Correctly handles both upward segments (joint above base) and downward segments (joint below base). Only upward segments require pushing when the knot moves up.
- **Topology Preservation**: Uses original positions to determine segment direction, preventing snap-back when knot moves past original joint position.

### Files Created
- `src/supports/PlacementLogic/ElasticChainSolver.ts`

### Files Modified
- `src/supports/SupportPrimitives/Knot/KnotGizmo.tsx` - Integrated elastic chain state capture and solver
- `src/supports/SupportPrimitives/Knot/KnotRenderer.tsx` - Added drag handlers (onDragStart/onDragEnd)
- `src/supports/SupportPrimitives/Knot/useKnotInteraction.ts` - Added elastic chain support (unused, gizmo handles drag)
- `src/supports/PlacementLogic/JointConstraintSolver.ts` - Added ignoredBranchIds parameter

## v72 - December 5, 2025
**Shaft Angle Constraint Implementation ✅**

### Shaft Angle Logic
- **Max Angle Setting**: Added a user-configurable `Max Angle` setting to the Shaft support settings (Defaults to 80°).
- **Core Constraint**: Updated `moveJoint` logic to clamp shaft angles relative to vertical (for trunks) or parent connection (for branches).
- **Smart Placement Integration**: Updated the pathfinding solver to respect the user's `maxAngleDeg` setting instead of a hardcoded value.

### Constraint Robustness
- **Branch Fix**: Implemented a robust fallback mechanism in `jointUtils.ts` to look up parent knots from the global store when moving branch joints. This solves the "V-shape" issue where branches could point downwards if their start context was missing.
- **Contact Cone Constraint**: Extended the angle constraint logic to the Contact Cone itself. The socket joint is now constrained relative to the fixed tip position on the model, preventing the cone from pointing downwards or exceeding the angle limit.

## v71 - December 5, 2025
**Branch Feature Parity & Knot Gizmo Implementation ✅**

### Knot Gizmo System
- **New KnotGizmo Component**: Implemented a dedicated gizmo for moving knots along their parent shaft with up/down arrow handles.
  - **Visual Design**: White arrows with cyan highlight on hover, rendered on top of all geometry using `depthTest={false}`.
  - **Direct Grab**: Added invisible grab sphere around knot allowing direct click-and-drag without needing to use arrow handles.
  - **Cursor Feedback**: Grab cursor appears on hover, grabbing cursor while dragging.
  - **GPU Picking Integration**: Registered gizmo with picking system to block hover highlights on supports underneath.

### Knot Position Synchronization
- **Critical Fix**: Knots now stay connected to trunk when joints are moved.
  - Added `calculateKnotPositionFromT()` utility to recalculate knot position from its `t` parameter.
  - Updated `updateTrunk()` in state.ts to automatically update attached knot positions when trunk geometry changes.

### Joint & Knot Interaction Improvements
- **Grab Cursor on Joints**: Added grab cursor when hovering over joints (when parent support is selected).
- **Direct Joint Dragging**: Joints can now be clicked and dragged directly (not just via gizmo handles).
  - Selecting a joint via drag also shows the gizmo for axis-constrained movement.
  - Left-click only - right-click is blocked to preserve camera orbit.
- **Smart Hitbox Sizing**: Joint/knot hitboxes only expand when parent is selected, preventing them from blocking support hover highlights.
- **Picking Registration**: Joints and knots only register with picking system when parent is selected, allowing support hover to work correctly.

### Visual & UX Polish
- **Knot Highlighting**: Knots now highlight on hover when parent branch is selected.
- **Branch Selection State**: Branch stays highlighted when its knot is selected.
- **KnotRenderer moved outside pickRef**: Allows independent picking registration for knots.

### Debug Cleanup
- Commented out console.log spam from: FootprintBorderRenderer, PickingProvider (register/unregister), usePickableModel, IslandOverlay.
- Disabled GPU picking debug mode (`gpuPickingTest={false}`).

## v70 - December 4, 2025
**LysConverter Socket Alignment Fix ✅**

### Contact Cone Alignment
- **Socket Joint Misalignment Fix**: Fixed a critical bug where imported Lychee supports had misaligned Contact Cones that required manual nudging to snap into place.
  - **Problem**: The socket joint position was calculated BEFORE the raycast snapped the tip to the model surface. This caused the cone's axis and socket position to be based on the pre-snap tip location, resulting in visible misalignment between the Contact Cone and Socket Joint.
  - **Solution**: After the raycast snaps the tip, the converter now recalculates the socket position using the same logic as `getFinalSocketPosition`:
    1. Calculate disk thickness offset using `calculateDiskThickness()`
    2. Find cone start position (tip + disk offset along surface normal)
    3. Calculate aligned socket position (cone start + tip length along cone axis)
    4. Update `socketJoint.pos` to this aligned position
- **Import**: Added `calculateDiskThickness` import from `ContactDisk/contactDiskUtils.ts`
- **Version Bump**: Updated to v13 logging for raycast diagnostics

## v69 - December 4, 2025
**Raycast Origin Fix for Hollow Geometry ✅**

### Hollow Model Support
- **Ray Origin Change**: Fixed raycast failing on supports inside hollow geometry (cylinders, tubes, etc.).
  - **Problem**: Ray origin was set 50mm behind the socket, causing it to hit the outer wall of hollow models before reaching the actual contact point inside.
  - **Solution**: Changed ray origin to start at the **Socket Joint** position instead of far behind it. Same direction, same angle - just closer starting point.
- **Impact**: Supports inside hollow models now correctly snap to the inner surface.

## v68 - December 4, 2025
**Lychee Surface Alignment (Raycast Snap) ✅**

### Ghost Mesh Fix
- **Contact Disk Snapping**: Implemented raycast-based surface snapping for imported Lychee supports. Contact Disks are now placed **flush** on the model surface with correct surface normals.
- **Critical Bug Fix (Center Offset)**: Fixed a major bug where the invisible "Ghost Mesh" used for raycasting was displaced from the visible model.
  - **Root Cause**: The visible `StlMesh` component uses a nested transform structure (`<group>` + `<mesh position={-center}>`), but the ghost mesh was only applying the group transform, missing the center offset.
  - **Solution**: Updated `useLycheeImport.ts` to create a `ghostGroup` + `mesh` hierarchy that replicates the exact transform structure of `StlMesh`.
- **Debug Logging (v12)**: Added comprehensive raycast diagnostics showing tip position, world bounding box, and hit/miss status.
- **Documentation**: Updated `MASTER_LysConversion_Bible.md` with new "Phase 1.5: Ghost Mesh for Surface Alignment" section.

## v67 - December 4, 2025
**Lychee Conversion Joint Diameter Fix ✅**

### Structural Consistency
- **Joint Diameter Alignment**: Fixed a discrepancy where imported Lychee joints were created with the exact same diameter as the shaft, making them visually indistinct and difficult to select.
- **Shared Utility Implementation**: Updated `LysConverter.ts` to import and use the shared `getJointDiameter` utility function (shaft + 0.1mm).
  - Applied to **Joint 0** (The Knee).
  - Applied to the **Socket Joint** (Connection to Contact Cone).
- **Impact**: Imported supports now respect Dragonfruit's "Building Codes" for component sizing while preserving Lychee's "Blueprints" for placement, ensuring full consistency with native supports.

## v66 - December 3, 2025
**Lychee Support Geometry & "Low Knee" Fix ✅**

### Geometry & Topology Logic
- **"Low Knee" Implementation**: Fixed the visual discrepancy where the first joint (Knee) appeared too high.
  - **Explicit Height**: Forced `Joint 0` to be placed exactly at the top of the Dragonfruit visual root structure (e.g., 0.8mm), ignoring Lychee's `baseTip.length` which caused disproportionate risers on scaled models.
  - **Buried Knee Fix**: Ensured the joint is not buried inside the root mesh by adding the full `totalBaseHeight` offset.
- **Vertical Priority Shafts**:
  - **Logic Update**: Updated Socket placement to prioritize vertical alignment with the Root, matching Lychee's visual style.
  - **Conditional Fallback**: The system attempts to align `Socket X/Y` with `Base X/Y` if the Contact Cone length permits it. If the Tip is too far horizontally, it gracefully falls back to a leaning projection.

### Coordinate System Fixes
- **Root Z Anchoring**: Fixed a bug where the Object's Z position was incorrectly added to the Root's Z position, causing supports to float or appear squashed. Roots are now strictly anchored to Z=0 (Floor).
- **Stability**: Reverted experimental X/Y transform changes to ensure reliable placement relative to the object origin.

## v65 - December 3, 2025
**Lychee Two-Step Import & Coordinate Alignment ✅**

### Import Workflow
- **Two-Step Flow**: Implemented a robust two-step import process (`useLycheeImport.ts`) requiring both the Lychee JSON (for data) and the original STL (for geometry).
  - **Phase Management**: Added state machine (`idle` -> `awaiting_stl` -> `processing`) to guide the user through the process.
  - **UI Integration**: Updated `IslandScanCard` to prompt for the STL file immediately after successful JSON parsing.

### Coordinate System Alignment
- **Transform Logic Fixed**: Solved the misalignment between the normalized STL geometry (Bottom-Centered) and Lychee's World Space.
  - **Full Offset Application**: Implemented `Final = Position + Center * Scale` for all axes. This accounts for Lychee's use of the `center` property as an intrinsic offset/lift, ensuring the model aligns perfectly with the supports.
  - **Auto-Lift Conflict**: Removed redundant auto-lift logic in `useTransformManager` that was interfering with the explicit transform set by the Lychee import.
- **Verification**: Confirmed that `Position.z=0` with `Center.z=20` and `Scale=0.25` correctly results in a 5mm lift, matching the support generation logic.

## v64 - December 2, 2025
**Lychee Slicer Reverse Engineering & Converter Core ✅**

### Coordinate System Solved
- **Reverse Engineering**: Successfully decoded the non-intuitive "Mixed Coordinate Space" used by Lychee Slicer supports.
  - **Tip (Contact)**: Uses **Local Object Space** (Scaled + Lifted + Centered). Verified against model surface (14.17mm).
  - **Base (Pillar Root)**: Uses **World Space** (Unscaled + Position Relative). Verified against visual placement (Outside model radius).
  - **Elbow Height**: Derived heuristic `BaseZ + TipLength + BaseLength` which matches observed slicer data within 0.1mm.

### Topology Reconstruction
- **Graph Structure**: Replicated the specific "Dog Leg" topology used by Lychee.
  - **Phantom Base**: Implemented the hidden "Zero-Height Segment" at the root (Root -> Lower) to support bottom-up editing behavior.
  - **4-Knot System**: Root (Floor), Lower (Floor), Elbow (Calculated), Tip (Model).
- **Converter Core**: Created `src/features/lys-conversion/LysConverter.ts` as a standalone service to transform raw JSON into compatible graph data structures.
- **Verification**: Validated geometry using a temporary `GhostOverlay` visualizer, confirming perfect alignment with user screenshots.

## v63 - December 1, 2025
**Bezier Gizmo Polish & Interaction Fixes ✅**

### Interaction Refinements
- **Right-Click Safety**: Disabled right-click interaction on Bezier gizmo handles to prevent accidental dragging while moving the camera.
- **Raycast Blocking**: Fixed an issue where hovering the gizmo handle would trigger support previews on the model behind it. Added event propagation stopping to `onPointerOver`, `onPointerOut`, and `onPointerMove`.

### Visual Feedback
- **Hover Highlight**: Added a Cyan (`#00FFFF`) highlight to the Bezier Handle spheres when hovered to clearly indicate interactivity.

## v62 - December 1, 2025
**Bezier Gizmo Implementation & Refinements ✅**

### Interactive Bezier Gizmo
- **New Tool**: Implemented a dedicated 3D gizmo for manipulating Bezier curve supports, mimicking vector design tools (Illustrator/Blender).
- **Visual Components**:
  - **Bezier Handles**: Added drag-able handles (Line + Sphere) at each end of a bezier segment.
  - **Seesaw Logic**: Implemented automatic "seesaw" behavior where rotating one handle updates the opposite handle to maintain smooth C1 continuity at the joint.
  - **Camera-Plane Drag**: Interaction is projected onto a plane facing the camera for intuitive 2D-style manipulation in 3D space.

### Usability & Visual Improvements
- **Handle Visibility**:
  - **Dual Handles**: Fixed logic to ensure handles appear at *both* ends of a selected segment (even for the bottom-most segment).
  - **Root Alignment**: Corrected the handle pivot for supports connected to Roots/Rafts. The handle now properly originates from the start of the shaft (above the cone/disk) rather than the build plate (Z=0).
- **Visual Tuning**:
  - **Shortened Handles**: Reduced default handle length by 30% to prevent visual clutter and overlapping handles in the middle of segments.
  - **Larger Grip**: Increased the control point sphere size by 2.2x to make them significantly easier to grab and see.

## v61 - November 30, 2025
**Major Export System Refactor - Eliminates Code Duplication ✅**

### Scene Objects Export Architecture
- **Actual Scene Export**: Replaced geometry regeneration with direct export of rendered scene objects
- **Zero Code Duplication**: Export now uses same geometry as visualization, eliminating maintenance overhead
- **Future-Proof System**: All new support features (contact disks, collision logic, etc.) automatically work in export
- **Ref-Based Architecture**: Added ref system to SupportRenderer exposing underlying THREE.Group to export system

### Component Chain Updates
- **SupportRenderer**: Converted to `forwardRef` with `useImperativeHandle` to expose group ref
- **SceneCanvas**: Added `supportsRef` parameter and passed through to SupportRenderer
- **ExportPanel**: Added `supportsRef` prop and passed to ExportManager
- **Main Page**: Created supports ref and passed through entire component hierarchy

### Export Scaling & Filtering Fixes
- **Scale Normalization**: Fixed support scaling issues by resetting scale to (1,1,1) and updating world matrices
- **Hitbox Mesh Removal**: Actually removed invisible hitbox meshes from export scene (STL exporter ignores visibility)
- **Material Filtering**: Filtered out transparent meshes and MeshBasicMaterials used for interaction hitboxes
- **Joint Sphere Fix**: Resolved massive joint spheres in export by removing 2.5x oversized hitbox geometry

### Technical Implementation
- **Clone & Clean**: Clone supports group then remove non-exportable meshes before STL generation
- **Raft Compatibility**: Maintained raft generation by preserving access to support state for footprint calculation
- **Matrix Updates**: Ensured proper world matrix transformations for accurate positioning
- **Type Safety**: Updated all ref types to handle null values properly

**Impact**: This refactor completely eliminates the need to update export geometry when adding new support features. The export will always match exactly what users see in the viewport, including all future enhancements.

## v60 - November 30, 2025
**Gizmo System Enhancements & User Experience Improvements ✅**

### Screen-Space Gizmo Implementation
- **Consistent Sizing**: Replaced `TransformGizmo` with `ScreenSpaceGizmo` for joint gizmo to maintain constant screen size regardless of camera distance
- **Customizable Scale Factor**: Added `scaleFactor` prop to `ScreenSpaceGizmo` allowing different gizmo sizes for different use cases
- **Joint Gizmo Sizing**: Set joint gizmo scale factor to 0.02 (half default) for better proportions

### Interaction Improvements
- **Right-Click Protection**: Prevented all gizmo handles from being right-clickable across move, rotate, scale, and center components
- **Camera Orbit Freedom**: Right-click now exclusively controls camera movement without accidental gizmo activation
- **Enhanced Gizmo Lines**: Extended arrow shaft length by 50% (4.0 → 6.0) and rotation ring radius by 50% (3.2 → 4.8) for better visibility

### Mode-Based Selection Management
- **Automatic Deselection**: Joint gizmo automatically deselects when switching between application modes (support → prepare → export)
- **Support-Mode Exclusivity**: Joint gizmo only visible and active in support mode, providing cleaner workflow
- **Clean State Transitions**: Selection doesn't persist across mode changes, ensuring fresh interaction state

## v59 - November 30, 2025
**Joint Deletion Safeguard & Contact Standoff Logic Refinement ✅**

### Critical Joint Protection
- **Top Joint Deletion Block**: Implemented safeguard in `src/supports/state.ts` to prevent deletion of the critical joint connecting the support shaft to the contact cone.
  - **Protection Logic**: Added check in `removeJoint()` function to block deletion when `trunk.contactCone.socketJointId === jointId`
  - **UI Coverage**: Protection applies to both programmatic calls and user interactions through `removeJointById`
  - **Structural Integrity**: Ensures the support structure maintains its connection between shaft and contact cone

### Collision-Based Standoff System
- **Extracted Collision Logic**: Moved collision-based standoff calculation from `moveJoint` to reusable `calculateSafeOffset` utility in `src/supports/PlacementLogic/CollisionAvoidance.ts`
- **Legacy Profile Migration**: Implemented smart legacy fix in `contactDiskUtils.ts` - treats old default max standoff of 1.5mm as 0.35mm for existing supports
- **Visual Alignment Fix**: Synchronized contact disk and cone rendering by passing `diskLengthOverride` from `ContactConeRenderer` to `ContactDiskRenderer`
- **Geometry Enhancement**: Updated `ContactDiskRenderer` to use cylinder shaft with spherical tip for clean joint appearance and flat base
- **Configurable Limits**: Exposed `maxStandoffMm` parameter in `SupportSidebar` UI with 0.35mm default for user tuning

## v57 - November 29, 2025
**Contact Cone Architecture Fix & Hotkey Refactor ✅**

### Contact Cone & Joint Logic
- **Socket Joint Decoupling**: Fixed a critical architectural issue where the "Socket Joint" (connection between Cone and Shaft) was sharing an ID with the "Knee Joint", causing them to move as one.
  - **Unique ID**: Updated `trunkBuilder.ts` to generate a dedicated unique ID for the socket connection.
  - **Explicit Joint Entity**: The Socket Joint is now a fully realized `Joint` object in the data structure, linked as the `topJoint` of the uppermost segment.
- **Dynamic Cone Interaction**: 
  - Updated `moveJoint` in `jointUtils.ts` to handle Socket Joint movement.
  - Moving the top joint now dynamically recalculates the **Contact Cone's Normal and Length**, allowing the cone to stretch and pivot while keeping the tip fixed on the model.
- **Visual Cleanup**: Removed duplicate joint rendering from `ContactConeRenderer`. The socket joint is now rendered by the standard `TrunkRenderer` as part of the skeleton, ensuring consistent interaction.

### Hotkey System Refactor
- **Centralized Config**: Created `src/hotkeys/hotkeyConfig.ts` as the single source of truth for all application hotkeys.
  - **Universal Keys**: Defined immutable system keys (Undo/Redo/Delete).
  - **Default Bindings**: Defined customizable feature keys (e.g., 'J' for Joint Creation).
- **Joint Creation Restoration**: Restored the missing 'J' key functionality by migrating the hook to the new system and linking it to `DEFAULT_KEYBINDINGS`.

## v56 - November 29, 2025
**Support Preview Interaction Fixes ✅**

### Interaction & Raycasting Fixes
- **Preview Disappearance from Below**: Fixed a critical bug where support placement previews would flicker or disappear when viewing the model from underneath.
  - **Cause 1**: The **Grid Helper** and **Axes Helper** were intercepting raycasts meant for the model, causing `onPointerOut` events when the cursor crossed grid lines.
  - **Cause 2**: The **Preview Mesh itself** (Shafts, Cones) was blocking the raycast to the model behind it, creating a self-canceling loop (Preview appears -> blocks ray -> preview disappears -> ray hits model -> preview appears).
  - **Cause 3**: The **Preview Joints** were registering with the GPU Picking System, causing the interaction manager to think the user was hovering an existing interactable element, which disabled placement logic.
- **Raycast Transparency**: 
  - Added `raycast={() => null}` to Grid and Axes helpers to make them "click-through".
  - Updated `SupportBuilder` to pass `raycast={() => null}` to all support primitives when in preview mode.
  - Added `enablePicking` prop to `JointRenderer` to disable GPU picking registration for preview joints.

## v55 - November 29, 2025
**Support Angle Gradient & Visualization Overhaul ✅**

### Visual Safety Feedback
- **3-Stage Color Gradient**: Implemented a 3-stage gradient for support previews to clearly communicate placement safety across the valid range (91° - 180°).
  - **Orange (91°)**: Risky placement (just past vertical).
  - **Yellow (120°)**: Warning/Transition zone.
  - **Green (180°)**: Safe placement (flat overhang).
- **Midpoint Skew**: Skewed the gradient midpoint to **120°** (instead of geometric center) to better highlight the transition from "Risky" to "Safe".
- **Angle Debug Overlay**: Added a billboarded **HTML Text Overlay** next to the support tip in preview mode.
  - Displays the exact surface angle in degrees (e.g., "95°").
  - Uses HTML to ensure text always faces the camera and remains legible regardless of 3D rotation.

### Logic Refinements
- **Strict Vertical Blocking**: Updated `StandardPlacement.ts` to shift the error threshold from 90° to **91°**.
  - **Effect**: Perfectly vertical surfaces (90°) are now consistently treated as **Errors** (Red/Blocked), resolving flickering where they could appear valid due to floating-point precision.
- **Fallback Angle Calculation**: Updated `SupportBuilder.tsx` to calculate the angle from the `contactCone` normal on-the-fly if the pre-calculated angle is missing from the data. This ensures the gradient and debug text always work, even if upstream logic drops the value.

### Documentation
- Updated `ARCHITECTURE_AND_HANDOFF.md` with the new Angle Classification table, Gradient details, and Visual Feedback architecture.

## v52 - November 29, 2025
**Voxel Alignment, Slicing Fixes & UI Decoupling ✅**

### Voxel Visualization Alignment
- **Independent Axis Offsets**: Introduced `VOXEL_OFFSET_X` and `VOXEL_OFFSET_Y` constants in `ScanOrchestrator.ts` to allow fine-tuning of voxel grid alignment.
- **Visual Correction**: Tuned offsets (X=0.5, Y=0) to perfectly align the island scan voxels and overlay markers with the source geometry, correcting a half-pixel shift issue.

### Slicing & Layer Slider Improvements
- **Global Scene Slicing**: Decoupled the layer slider from the *active model selection*. Slicing now operates on the **global scene bounds** (union of all visible models), ensuring the slider remains functional and consistent even when deselecting models.
- **Persistence**: Removed the conditional rendering that caused the layer slider to disappear when switching modes or deselecting models.
- **Cross-Section Cap Fixes**:
  - Restored visibility by correctly using `clipUpper` (top cut plane) instead of the unused `clipLower`.
  - Fixed the **Rasterized Mode** toggle (right-click on slider) by wiring up missing `crossSectionMode` and `pxMm` props to `SceneCanvas`.
  - **Color Standardization**: Forced the cross-section cap to render in **Pure White** (`#FFFFFF`) instead of inheriting the model's color, improving visibility.

## v51 - November 28, 2025
**Detached Export System & Geometry Accuracy ✅**

### Detached Export Architecture
- Implemented a pure-logic export system in `ExportManager.ts` that reconstructs the scene from data rather than exporting React components.
- Created `SupportGeometryGenerator.ts` to replicate `RootsRenderer`, `ShaftRenderer`, `JointRenderer`, and `ContactConeRenderer` logic in pure Three.js code.
- **Why**: Decouples export from UI state, ensures Z-up orientation (STL standard), and allows for future Web Worker offloading.

### Geometry & Positioning Fixes
- **Coordinate Alignment**: Fixed the "offset model" issue by replicating `StlMesh`'s center-offset logic in the export scene. Supports now align perfectly with the model.
- **Orientation Fix**: Applied 90° X-rotation to all cylinders and cones to match the Z-up world coordinates (Three.js cylinders default to Y-up).
- **Contact Cones**: Implemented full contact cone export with correct tapering and socket sphere generation.
- **Raft Awareness**: Export generator now accepts `RaftSettings`. Roots are automatically lifted and resized (thin disk) when a raft is enabled to match the renderer's visual logic.

### Documentation
- Updated `ARCHITECTURE_AND_HANDOFF.md` with a new "Export System" section detailing the detached architecture.
- Updated `NewSupportTypeGuide.md` to include a checklist item for updating `ExportManager` when adding new support types (e.g., Branches).

## v50 - November 28, 2025
**Warning System Refinement & UI Polish ✅**

### Warning System Adjustments
- **Angle Warning Threshold**: Adjusted the threshold for triggering the "Stability Warning" from 110° to **120°**.
  - **< 90°**: Error (Red) - Upward facing surfaces (blocked).
  - **90° - 120°**: Warning (Yellow) - Vertical walls to 30° overhangs (allowed with warning).
  - **> 120°**: Good (Green) - Safe overhangs.
- **Smart Placement Integration**: Fixed a bug where `SmartPlacement.ts` was dropping warnings from valid standard placements, causing risky angles to appear Green. Warnings are now preserved.

### UI & Interaction Polish
- **Transparent Error Previews**: Added `PREVIEW_ERROR_OPACITY` (0.25) to `SupportBuilder.tsx`. Red "Error" previews are now 50% more transparent than Green/Yellow previews to be less visually overwhelming.
- **Tooltip Fixes**:
  - Updated `SceneCanvas.tsx` to prevent the "Cannot Place Support" tooltip from appearing when hovering over an existing support (using GPU picking to detect support/gizmo category).
  - Wrapped `SupportLimitationFeedback` to ensure tooltips are hidden when support placement is blocked (e.g., during gizmo drag).
- **Documentation**: Updated `ARCHITECTURE_AND_HANDOFF.md` with the new Limitation and Warning codes and messages.

## v49 - November 28, 2025
**Smart Support Placement (Collision Avoidance) ✅**

### Collision Detection & Avoidance
- Implemented `SmartPlacement.ts` logic module to automatically navigate supports around obstacles.
- **Iterative Solver**: Uses a multi-step approach to add "Knees" (joints) when a straight path is blocked.
- **Compass Search**: When blocked, tests 8 directions (360°) around the collision point to find the best escape path.
- **Multi-Radius Search**: Tests variable offsets (`[2, 5, 10, 15, 20]mm`) to find the tightest turn that clears the obstacle.
- **High Knee Strategy**: Prioritizes placing the turn near the start (Socket) to create an immediate "Elbow" rather than a late turn deep in the stack.

### Robustness & Safety
- **CollisionUtils**: Implemented a shared raycasting utility with **9-Ray Whiskers** (Center + 8 Perimeter) to detect grazing collisions on thin shafts.
- **Safety Margin**: Enforces a **0.25mm** clearance buffer around the model to prevent fusing due to light bloom.
- **Angle Constraints**: Enforces a maximum overhang angle of **70°** from vertical. Allows exceptions for short horizontal connectors (< 2mm).

### Architecture Updates
- Updated `trunkBuilder.ts` to accept an optional `mesh` parameter and delegate to `SmartPlacement` when present.
- Refactored `trunkBuilder` to support dynamic segment generation (N-Joints) based on the solver result.
- Updated `ARCHITECTURE_AND_HANDOFF.md` with the new Smart Placement system details.

## v48 - November 27, 2025
**Support-Model Linkage & Cascading Deletion ✅**

### Support-Model Linkage System
- Introduced `SupportEntity` contract in `types.ts` to mandate `modelId` on all support types (Trunk, Branch, Roots, Brace).
- Implemented `SupportModelLinker.ts` to centralize logic for querying and deleting supports by model ID.
- Updated `trunkBuilder.ts` and `useTrunkPlacement.ts` to capture `modelId` from the raycasted mesh's `userData` during placement.

### Cascading Deletion & Safety
- `useSceneCollectionManager` now calls `SupportModelLinker.deleteSupportsForModel` when a model is removed, ensuring supports don't float in mid-air.
- `SceneCanvas` now passes `userData={{ modelId }}` to meshes so raycasting can identify the model.
- `removeTrunk` in `state.ts` now safely clears the global selection if the deleted support (or its parts) was active.

### Developer Documentation
- Created `1. Documentation/LogicBible/NewSupportTypeGuide.md` as the master guide for creating new support types, covering data contracts, builder patterns, and system integration.

## v47 - November 27, 2025
**Global Delete Flow + STL Reload Fix ✅**

### Keyboard Delete Flow
- Added `src/features/delete/deleteRegistry.ts` for priority-based handler registration plus `useDeleteHotkey` mounted in `page.tsx`.
- Support domain now registers delete handlers that remove selected joints (merging segments + history) or entire trunks/roots.
- Scene manager registers a lower-priority handler to clear the active STL in Prepare mode, revoking blob URLs and hiding the mesh.
- Architecture guide updated with “Phase 6” documentation covering registry, priorities, and handler scopes.

### STL Reload UX
- `useSceneManager` now resets the hidden file input and forces `meshVisible=true` when a new STL loads, so deleting and immediately reloading the same file works reliably.

## v46 - November 27, 2025
**Undo/Redo Infrastructure + Prepare Mode Controls ✅**

### Global History & Hotkeys
- Added shared history domain (`src/history/`) with push/undo/redo APIs and handler registry.
- Wired global hotkeys (`Ctrl/Cmd+Z`, `Ctrl/Cmd+Shift+Z`) via `src/hotkeys/useUndoRedoHotkeys.ts` and mounted in `page.tsx`.
- Introduced support history handlers (`src/supports/history/useSupportHistoryHandlers.ts`) so trunk add/remove/update actions can be reversed.
- `useTrunkPlacementV2`, `useJointInteraction`, and `JointGizmo` now push history snapshots for placements and joint drags.

### Prepare Mode Toolbar Layout
- Restored the floating Select/Transform buttons (`TransformToolbar`) and ensured the transform panel aligns with the top bar + sidebar by offsetting the scene container (`ml-72 pt-14 ...`).
- Updated `ARCHITECTURE_AND_HANDOFF.md` with the history phase and Prepare mode control notes.

## v45 - November 27, 2025
**Major Architecture Refactor: Settings, SupportBuilder, Primitives & Types ✅**

### Support Settings System
- Created `src/supports/Settings/` module for user-adjustable support settings
  - `defaults.ts` - Single source of truth for default values (easy to edit by hand)
  - `types.ts` - `SupportSettings`, `TipProfile`, `ShaftProfile`, `RootsProfile`, etc.
  - `state.ts` - Settings store with subscription pattern
  - `presets.ts` - Built-in presets (Detail, Structure, Anchor)
  - `SupportSidebar.tsx` - Main settings panel UI
  - `components/` - UI sub-components (TipSettingsCard, ShaftSettingsCard, etc.)
- Removed hardcoded constants from `constants.ts`, now only contains derived values
- Settings flow: User adjusts sidebar → store updates → placement reads `getSettings()`

### SupportBuilder Pattern (Unified Preview/Placement)
- Created `src/supports/rendering/SupportBuilder.tsx` - Generic renderer for any support
  - Takes `SupportData` and `isPreview` prop
  - Renders whatever primitives are in the data (roots, shafts, joints, contactCone)
  - Preview uses transparent green material, placement uses normal material
- Created `src/supports/SupportTypes/Trunk/trunkBuilder.ts`
  - `buildTrunkData()` defines what a trunk is made of
  - Same function used for both preview AND placement (no duplication)
- Deleted `TrunkPlacementPreview.tsx` - No longer needed
- Added `transparent` and `opacity` props to all primitive renderers

### Folder Reorganization
- Created `src/supports/SupportPrimitives/` - Building blocks for all supports
  - Moved `Roots/`, `Shaft/`, `Joint/`, `ContactCone/` into this folder
  - These are the primitive parts that combine to form support types
- Created `src/supports/SupportTypes/` - Support type definitions
  - Moved `Trunk/`, `Branch/` into this folder
  - Future: `Twig/`, `Stick/`, `Brace/`
- Updated all import paths throughout codebase

### Architecture Documentation
- Updated `ARCHITECTURE_AND_HANDOFF.md` with new folder structure
- Added Support Type Definitions table (what primitives each type uses)
- Documented SupportBuilder pattern

**Files Created:**
- `src/supports/Settings/defaults.ts`
- `src/supports/Settings/types.ts`
- `src/supports/Settings/state.ts`
- `src/supports/Settings/presets.ts`
- `src/supports/Settings/SupportSidebar.tsx`
- `src/supports/Settings/components/*.tsx` (6 files)
- `src/supports/Settings/index.ts`
- `src/supports/rendering/SupportBuilder.tsx`

## v100 - December 21, 2025
**Anatomy Preview: Dynamic Camera Zoom (Tip & Cone) + Live Reactivity Fixes ✅**
